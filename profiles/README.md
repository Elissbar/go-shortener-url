- Бенчмарки показали, что предварительная аллокация ( make([]int, 0, size) ) ускоряет выполнение кода и уменьшает кол-во аллокаций, нежели переаллокация в цикле
хотя pprof в тоже время показал, что с предварительной аллокацией параметр alloc_space увеличился.
 
- Декодирование данных из запроса с помощью json.Unmarshal() работает чуть быстрее нежели создание декодера json.NewDecoder().Decode().
